# NewsLens-AI Frontend Environment Configuration
# When running with Vite dev server or Nginx proxy, requests to /api are proxied to the backend.
# Set VITE_API_BASE_URL if connecting directly to an external backend host.
VITE_API_BASE_URL=
